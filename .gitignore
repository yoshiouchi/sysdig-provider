# Local .terraform directories
**/.terraform/*

# Terraform state files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude override files as they are usually local only
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include lock file to track provider versions
# (keep committed once each env is initialized)
!terraform.lock.hcl

# Ignore CLI config files (sensitive)
.terraformrc
terraform.rc

# Ignore plan output files
*.tfplan
